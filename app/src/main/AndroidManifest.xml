<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.swu.likelion">
    <!-- 위치권한 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_icon"
        android:label="Honey Way"
        android:largeHeap="true"
        android:roundIcon="@drawable/logo_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PopUpActivity" android:theme="@style/Dialog"></activity>
        <activity android:name=".DFSActivity" />
        <activity android:name=".FiveRoadActivity" />
        <activity android:name=".StationMapAActivity" />

        <meta-data
            android:name="com.samsung.android.icon_container.has_icon_container"
            android:value="true" />

        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        -->
        <activity android:name=".MainActivity" />
        <activity android:name=".ResultActivity" />
        <activity android:name=".DetailActivity" />
        <activity android:name=".CongestionActivity" />
        <activity android:name=".SelectActivity" /> <!-- <activity android:name=".SplashActivity"></activity> -->
        <activity android:name=".Select2Activity" />
        <activity android:name=".StationMapDActivity" />
    </application>

</manifest>